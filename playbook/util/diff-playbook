function cleanup {
  rm -fr "$DFILE"
}
trap cleanup EXIT

HERE=`pwd`
DFILE=`mktemp -d`
TFILE="archive.tar.gz"
cd $DFILE
slapcache-download --destination=$TFILE
tar -xzf $TFILE
rm $TFILE

diff -ruw $HERE $DFILE
